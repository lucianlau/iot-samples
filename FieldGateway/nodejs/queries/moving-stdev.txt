SELECT      id,
            System.Timestamp AS sysTime,
            AVG(temperature) AS avg_temperature,
            STDEV(temperature) AS stdev_temperature
INTO        output
FROM        input
GROUP BY    TumblingWindow(s, 5), id